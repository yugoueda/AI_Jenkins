# コード品質チェックリスト

- `async/await` が適切に使用され、未awaitの `Future` がない
- `BuildContext` を非同期処理をまたいで使用していない
- `late` 変数が初期化前にアクセスされる可能性がない
- null安全を壊す `!` の使用に妥当性がある
- Widgetの `build` メソッドに副作用がない
- `setState` の呼び出しが適切なスコープ内にある
- 関数・Widget・クラスの責務が分離されている
- 重複コードが過剰に増えていない
- 境界値、空配列、null、例外系の扱いがある
